package xyz.elevated.frequency.check.impl;

import xyz.elevated.frequency.check.type.RotationCheck;
import xyz.elevated.frequency.data.PlayerData;

public class Test extends RotationCheck {

    public Test(PlayerData playerData) {
        super(playerData);
    }
}
